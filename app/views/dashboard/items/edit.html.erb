<%= form_for @item, url: "/dashboard/items/#{@item.id}" do |f| %>
<% if @item.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>

          <ul>
          <% @item.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
    <% end %>
<%= f.label :item_name%>
<%= f.text_field :item_name%>
<%= f.label :image_url%>
<%= f.text_field :image_url%>
<%= f.label :inventory%>
<%= f.number_field :inventory, min: 0.0%>
<%= f.label :price%>
<%= f.number_field :price, min: 0.0%>
<%= f.label :description%>
<%= f.text_field :description%>
<%= f.submit 'Edit Item' %>
<% end %>
