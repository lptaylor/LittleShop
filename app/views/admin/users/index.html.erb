<section class="jumbotron">
<h1>All Registered Users</h1>
</section>

<% @users.each do |user|%>
<div id="user-<%= user.id %>">
  <%= link_to user.name, admin_user_path(user.id) %>
  <%= user.created_at %>
  <%if user.active == true %>
    <%= button_to "disable", admin_user_path(user.id), method: :patch %>
  <% else %>
    <%= button_to "enable", admin_user_path(user.id), method: :patch %>
  <% end %>
</div>
<%end%>
